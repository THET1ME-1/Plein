<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Вырез экрана появился в Android 8.1: на восьмёрке атрибута нет вовсе, и
      в общей теме он держал проверку красной. Здесь лаунчер занимает и полосу
      под вырезом — фон обязан идти до самого края.
    -->
    <style name="Theme.Plein" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentTransitions">false</item>
    </style>
</resources>
